import { ToggleTaskItemMenuDirective } from './toggle-task-item-menu.directive';

describe('ToggleTaskItemMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleTaskItemMenuDirective();
    expect(directive).toBeTruthy();
  });
});
